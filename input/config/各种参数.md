    % 掩模参数
    params.Lx = 2048;
    params.Ly = 2048;
    params.maskSizeX = 1024;
    params.maskSizeY = 1024;
    params.maskType = 'Import';     % 'Import' or 'LineSpace'
    params.lineWidth = 128;
    params.spaceWidth = 128;
    params.isHorizontal = false;
    params.maskFile = 'mask/6T_SRAM.bin';
    params.dose = 1.0;

    % 光源参数
    params.srcSize = 101;
    params.srcType = 'Annular';     % 'Annular' or 'Circular'
    params.annular_inner = 0.6;
    params.annular_outer = 0.9;

    % 光学参数
    params.NA = 0.8;
    params.wavelength = 193;        % nm
    params.defocus = 0.2;

    % kernel 参数
    params.nk = 10;                  % 保留 kernel 数
    params.targetIntensity = 0.225; % 二值化阈值