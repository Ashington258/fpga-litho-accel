提取出最小的单元，不需要生成掩膜和光源
关于TCC模块，只需要拿到输入参数(光源和掩膜的bin，以及光学参数)，然后就执行计算，输出空中像和SOCS kernel
关于SOCS模块，只需要拿到SOCS kernel和mask，


文件结构说明



现在想把TCC计算模块`reference/CPP_reference/Litho-TCC`重构到vitis HLS C++平台(参考`reference/vitis_hls_ftt的实现/interface_stream`)
关于TCC模块，只需要拿到输入参数(`input文件夹`光源和掩膜的bin，以及光学参数)，然后就执行计算，输出空中像和SOCS kernel
请先写一个重构的todo，我来评审一下

重点重构的部分：
核心重构模块（硬件加速部分）
calcTCC 函数 (核心优先级最高)：

理由： 包含四层嵌套循环，涉及大量的复数乘加运算、平方根和三角函数。这是整个仿真的性能瓶颈。

重构重点： 将循环展开（Unrolling）和流水线化（Pipelining）。由于 pupil 矩阵和 tcc 矩阵较大，需要合理设计 BRAM 或 URAM 的存储排布。

FT_r2c 和 FT_c2r (FFT 部分)：

理由： 目前代码调用的是 fftw3 库，这是 CPU 端的库。

重构重点： 替换为 Vitis HLS DSP Library 中的 hls_fft.h 或调用 Xilinx 的 FFT IP 核。硬件 FFT 通常采用流式处理（Streaming）。

calcImage 函数：

理由： 典型的频域卷积操作，适合 FPGA 的空间并行性。

calcKernels (特征值分解)：

注意： 该函数调用了 LAPACK 的 zheevr_。在 HLS 中实现完整的特征值求解非常复杂。通常建议**保留在 CPU 端（Host）**计算，除非 nk（卷积核数量）非常大且需要极高性能。



---


1. 尽可能的准确预估xcku3p的BRAM<Block RAM (BRAM)：12.7 Mb>是否能完成目前默认参数的计算
2. 协调出一个合适的参数，并修改`reference/CPP_reference/Litho-TCC/klitho_tcc.cpp`的参数，基于这个程序写一个验证脚本，通过这个脚本的计算结果可以核对HLS的计算是否正确

先完成这样的一个验证脚本，脚本方便调用，且debug log信息充足，方便及时的给到copilot调试代码

最终的协同结果放入到config并同步更新全局约束`.github/copilot-instructions.md`
并且这个测试脚本同时也是从config读取参数



----

